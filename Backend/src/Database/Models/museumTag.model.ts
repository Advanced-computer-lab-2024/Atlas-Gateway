import { schemaConfig } from "#utils";
import { Document, Schema, Types, model } from "mongoose";

// These tags will be basically a key-value pair, since the name of the tag is undefined and the value is also undefined
interface IMuseumTag extends Document{

}